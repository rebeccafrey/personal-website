{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "my personal website",
  "main": "index.html",
  "scripts": {
    "start": "npm run sass && npm-run-all --parallel sass:watch serve",
    "sass": "node-sass --output-style compressed --source-map true scss/styles.scss docs/styles.css",
    "sass:watch": "npm run sass -- -w",
    "serve": "serve docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.1",
    "npm-run-all": "^4.1.5",
    "serve": "^11.3.0"
  }
}
